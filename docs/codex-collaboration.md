# Human–Codex collaboration

## Origin and human direction

The creator supplied the real problem: at an international summer school, lectures and conversations in English were difficult to understand, yet dedicated study time was scarce. The creator noticed a second recurring problem—short waits while AI tools worked—and chose to connect them.

The human decisions include the one-question waiting-time loop, immediate pause when AI work completes, local-first history, no punitive daily streak, Chrome as the first platform, the Education category, the lecture-preparation story, and the non-negotiable boundary that AIterval never reads AI prompts or responses.

## What Codex contributed

Most core implementation and the Build Week upgrade were completed in this same primary Codex task. Codex translated the product brief into the workspace architecture, semantic site adapters, deterministic state machine, schemas and migrations, React surfaces, local recommendation logic, server-only GPT-5.6 integration, Structured Outputs, import boundary, privacy controls, tests, deployment configuration, and submission material.

Codex also helped by running the real build and Chromium suites, diagnosing environment and locator failures, comparing extension permissions and CSP, checking official requirements, keeping claims evidence-based, and preparing reproducible release/deployment checks. The creator remained responsible for the lived insight, product priorities, final review, real user testing, private credentials, video narration/upload, and submission.

## Review boundary

Codex did not invent a user-test result, testimonial, live API call, deployment status, judge invitation, or model session identifier. Curated demo data is visibly labeled. Human-only steps remain unchecked until performed.

## Required Build Week feedback identifier

Primary Codex task `/feedback` Session ID: **[PASTE REAL SESSION ID AFTER RUNNING `/feedback`]**

The creator must run `/feedback` in the same primary task after final review, then copy the real value here, into `docs/devpost-submission-en.md`, and into Devpost. Do not replace this placeholder with an inferred thread ID.
